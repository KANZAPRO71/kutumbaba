# Copy to keystore.properties (never commit). Used for release signing + Play upload.
storeFile=../release/papua-ai-upload.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=upload
keyPassword=YOUR_KEY_PASSWORD
