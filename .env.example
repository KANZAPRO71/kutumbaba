# URL of the upstream restaurant API that the proxy will forward requests to.
# Replace this with your actual API endpoint.
RESTAURANTS_API_URL=https://your-restaurant-api.example.com/restaurants

# Port the proxy server will listen on (default: 3000)
PORT=3000
