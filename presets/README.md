# Presets (development mirror)

Canonical preset files ship inside the installed package:

`src/persona_ai/presets/`

Runtime code loads presets package-relatively via `persona_ai.presets`.
These root-level files are kept for convenient editing during development only.
